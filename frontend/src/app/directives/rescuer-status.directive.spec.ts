import { RescuerStatusDirective } from './rescuer-status.directive';

describe('RescuerStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new RescuerStatusDirective();
    expect(directive).toBeTruthy();
  });
});
